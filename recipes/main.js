// import { recipes } from "./recipes.mjs";

// function addArticles() {
//     var articleGrid = document.querySelector('.article-grid');
//     recipes.forEach(article => {
//       var htmltoinsert = articleTemplate(recipes.author, recipes.url, recipes.isBasedOn, recipes.cookTime, recipes.datePublished, recipes.tags, recipes.description, recipes.image, recipes.recipeIngredient, recipes.name, recipes.prepTime, recipes.recipeInstructions, recipes.recipeYield, recipes.rating);
//       var articleElement = document.createElement('div');
//       // articleElement.innerHTML = htmltoinsert;
//       // articleGrid.appendChild(articleElement);
//       articleGrid.insertAdjacentHTML('beforeend', htmltoinsert)
//     });
//   };
  
//   document.addEventListener('DOMContentLoaded', addArticles)